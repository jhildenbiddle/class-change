{
  "name": "class-change",
  "version": "0.0.4",
  "description": "Simplified CSS class change event delegation and multi-class add, remove and toggle methods",
  "author": "John Hildenbiddle (http://hildenbiddle.com)",
  "license": "MIT",
  "homepage": "https://github.com/jhildenbiddle/class-change#readme",
  "repository": {
    "type": "git",
    "url": "git+https://jhildenbiddle@github.com/jhildenbiddle/class-change.git"
  },
  "bugs": {
    "url": "https://github.com/jhildenbiddle/class-change/issues"
  },
  "keywords": [
    "css",
    "change",
    "class",
    "classChange",
    "classList",
    "delegate",
    "event",
    "polyfill"
  ],
  "dependencies": {},
  "devDependencies": {
    "browserify": "^13.0.0",
    "uglifyify": "^3.0.1"
  },
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:dev": "browserify index.js -s classChange > dist/class-change.js",
    "build:min": "browserify -g uglifyify index.js -s classChange > dist/class-change.min.js",
    "prebuild": "mkdir -p dist/",
    "build": "npm run build:dev && npm run build:min"
  }
}
