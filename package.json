{
  "name": "class-change",
  "version": "1.0.3",
  "description": "A small, dependency-free micro-library for manipulating CSS class names and generating class change event listeners.",
  "author": "John Hildenbiddle (http://hildenbiddle.com)",
  "license": "MIT",
  "homepage": "https://github.com/jhildenbiddle/class-change#readme",
  "repository": {
    "type": "git",
    "url": "git+https://jhildenbiddle@github.com/jhildenbiddle/class-change.git"
  },
  "bugs": {
    "url": "https://github.com/jhildenbiddle/class-change/issues"
  },
  "keywords": [
    "change",
    "class",
    "classChange",
    "classList",
    "css",
    "delegate",
    "delegation",
    "event",
    "listener",
    "polyfill"
  ],
  "main": "src/index.js",
  "scripts": {
    "build": "webpack --hide-modules",
    "clean": "rimraf dist/*",
    "serve": "browser-sync start --server 'demo' --files 'demo,dist' --serveStatic 'dist' --reload-on-restart --no-open --no-notify",
    "start": "npm run clean && npm run watch & npm run serve",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "nodemon package.json & npm run build -- --watch"
  },
  "devDependencies": {
    "browser-sync": "^2.14.0",
    "deepcopy": "^0.6.3",
    "nodemon": "^1.10.0",
    "rimraf": "^2.5.4",
    "webpack": "^1.13.1"
  }
}
